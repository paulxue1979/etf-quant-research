# ETF Quant Research System V1.2 Release Gate

## Release Scope

V1.2 adds chart-wide navigation and a versioned multi-strategy research comparison workflow. The approved scope is:

- synchronized base-100 TWR and drawdown comparison charts;
- categorized metrics with deterministic sorting;
- stable strategy colors, legend hide/show, focus mode, and Show All;
- compatibility states for comparable, warning, unknown, and incompatible runs;
- MAX, Fit All, Reset View, shared range, and shared crosshair controls;
- clearer stateful-strategy starting allocation and hold-previous behavior;
- discoverable one-time and monthly capital contributions;
- long-history and ten-run browser hardening.

The comparison wealth index is normalized to 100 at each run's first available point. It is a time-weighted strategy-performance view and does not replace Portfolio Value, Capital Invested, Profit, or investor XIRR. Drawdown is sourced from the canonical backend series. Compatibility warnings remain visible, and an incompatible result is not presented as a fair strategy comparison.

## Contribution And Stateful Semantics

Capital contributions are external cash flows, not strategy signals. A requested contribution enters Cash on its effective common trading date and is deployed only through the current strategy target and rebalance policy. TWR removes the timing effect of external cash flows; XIRR describes the investor experience.

Starting Allocation seeds a stateful strategy only before its first resolved target. HOLD_PREVIOUS keeps the most recently resolved target, does not reapply Starting Allocation, and does not create a synthetic signal. Signal execution remains `Signal(T) -> T+1 open`.

## Gate Evidence

- Baseline: `44c35cc780c3de5cb1b719b027de21a5671f2519` (`fix: harden long-history comparison ux`).
- Backend non-network regression: `791 passed, 4 deselected`.
- V1.2 focused backend regression: `85 passed`.
- Frontend regression: `110 passed`.
- TypeScript, ESLint, Vite production build, Ruff lint, and Python compile/import: PASS.
- Real Tiingo integration: `3 passed` for QQQ, TQQQ, and SPY.
- Five-ETF real-data acceptance: `1 passed` for QQQ, TQQQ, SPY, IWM, and SGOV using `ADJUSTED_CLOSE`.
- Real browser: 2000-present report data, ten-run comparison, range controls, legend visibility, focus mode, sorting, DCA controls, stateful controls, and zero console errors/warnings were exercised.
- Long-history report: 6,539 daily points from 2000-01-03 through 2025-12-31; no downsampling was required.
- The 390x844 real-browser remediation passed with no page-level horizontal overflow. The metrics table scrolls inside its own bounded container; comparison controls, contribution controls, and stateful controls remain usable.
- A real persistent comparison matrix now contains QQQ Buy & Hold, QQQ SMA200, and canonical QQQ SMA200 Hysteresis runs. All use 2000-01-03 through 2025-12-31, `ADJUSTED_CLOSE`, USD 100,000 initial capital, zero commission/slippage, integer shares, `Signal(T) -> T+1 open`, and SPY benchmark.
- The real comparison correctly reports `WARNING` for differing data provenance and unavailable immutable dataset versions. It is not represented as `COMPARABLE`.
- The 2000-present Buy & Hold browser run exposes Portfolio Value, Capital Invested, strategy and SPY TWR, strategy and SPY drawdown, strategy provenance, target and actual allocation, holdings, MAX, Fit All, and Reset View in one immutable BacktestRun.

## Known Limitations

- The current research-history endpoint deserializes and sorts all stored runs before pagination; loading 129 large runs took about 16 seconds in this gate.
- The research-history performance was rechecked after remediation: HTTP 200 with a 14.44-second first load. It did not crash, freeze the browser, alter backtest execution, or change comparison results.
- Repository-wide Ruff formatting debt remains: the same 45-file set fails at `v1.1.0` and at the V1.2 HEAD. The set difference is empty. All four Python files changed between `v1.1.0` and V1.2 pass `ruff format --check`, and full-repository Ruff lint passes.
- The Vite build retains the existing warning for a JavaScript chunk larger than 500 kB.

## Deferred

- weekly and intraday research;
- optimization, grid search, walk-forward, and automatic best-strategy selection;
- bundle architecture rewrite;
- immutable dataset snapshot redesign;
- Portfolio Value comparison expansion.

## Release Decision

No CRITICAL or HIGH functional or research-integrity regression was found. Automated behavior, real Tiingo data, five-ETF research flow, 390x844 browser behavior, the real strategy matrix, and the 2000-present benchmark report pass. The 45-file Ruff format finding is proven pre-existing debt; no V1.2 Python file is unformatted.

**ETF Quant Research System V1.2 READY FOR RELEASE.**

No `v1.2.0` tag was created during this gate.
